import { useEffect, useMemo, useState } from 'react'
import { ArrowLeft, ArrowRight, BookOpen, Check, CheckCircle2, Download, FileText, ListVideo, MessageCircle, Pause, Play, RotateCcw, Sparkles, StickyNote } from 'lucide-react'
import { Link, useNavigate, useParams } from 'react-router-dom'
import { AIChatbot } from '../components/AIChatbot'
import { Icon } from '../components/Icon'
import { accountingVideos } from '../data/roles'

type PlayerTab = 'notes' | 'attachments' | 'assistant'

export function VideoPlayerPage() {
  const { videoId } = useParams()
  const navigate = useNavigate()
  const video = accountingVideos.find((item) => item.id === videoId) ?? accountingVideos[0]
  const currentIndex = accountingVideos.findIndex((item) => item.id === video.id)
  const nextVideo = accountingVideos[currentIndex + 1]
  const [progress, setProgress] = useState(video.completed ? 100 : 42)
  const [isPlaying, setIsPlaying] = useState(false)
  const [isCompleted, setIsCompleted] = useState(Boolean(video.completed))
  const [activeTab, setActiveTab] = useState<PlayerTab>('notes')
  const [notes, setNotes] = useState('')

  useEffect(() => {
    setProgress(video.completed ? 100 : 42)
    setIsPlaying(false)
    setIsCompleted(Boolean(video.completed))
    setActiveTab('notes')
    setNotes('')
  }, [video.id, video.completed])

  const watchedLabel = useMemo(() => `${Math.floor((progress / 100) * Number(video.duration.split(':')[0]))} phút đã xem`, [progress, video.duration])

  const handleNext = () => {
    if (nextVideo) navigate(`/dashboard/learn/${nextVideo.id}`)
    else navigate('/dashboard')
  }

  const markComplete = () => {
    setProgress(100)
    setIsCompleted(true)
  }

  return <>
    <section className="mx-auto max-w-7xl px-5 py-8 sm:px-8 lg:px-10 lg:py-12"><div className="flex flex-wrap items-center justify-between gap-4"><Link to="/dashboard" className="inline-flex items-center gap-2 text-sm font-semibold text-slate-500 transition hover:text-slate-900 dark:text-slate-400 dark:hover:text-white"><ArrowLeft size={15} /> Dashboard học tập</Link><span className="inline-flex items-center gap-2 text-xs font-medium text-slate-400"><ListVideo size={15} /> {currentIndex + 1} / {accountingVideos.length} video</span></div><div className="mt-8 grid gap-8 lg:grid-cols-[minmax(0,1fr)_350px] lg:items-start">
      <div className="min-w-0">
        <div className="relative aspect-video overflow-hidden rounded-[1.75rem] bg-slate-950 shadow-2xl shadow-slate-900/15"><div className="absolute inset-0 bg-[radial-gradient(circle_at_68%_35%,rgba(45,212,191,0.28),transparent_26%),linear-gradient(135deg,#0f172a,#0b292d)]" /><div className="absolute inset-0 opacity-20" style={{ backgroundImage: 'linear-gradient(rgba(148,163,184,.2) 1px, transparent 1px), linear-gradient(90deg, rgba(148,163,184,.2) 1px, transparent 1px)', backgroundSize: '32px 32px' }} /><div className="absolute left-5 top-5 flex items-center gap-2 rounded-full border border-white/10 bg-white/10 px-3 py-1.5 text-[10px] font-bold uppercase tracking-[0.16em] text-teal-200 backdrop-blur">{video.module}<span className="h-1 w-1 rounded-full bg-teal-300" />AI for Accounting</div><div className="absolute inset-0 flex flex-col items-center justify-center px-6 text-center text-white"><span className="flex h-16 w-16 items-center justify-center rounded-3xl bg-teal-300 text-slate-950 shadow-xl shadow-teal-300/20"><Icon name={video.icon} size={29} /></span><p className="mt-5 text-xs font-semibold uppercase tracking-[0.2em] text-teal-200">Lesson preview</p><h1 className="mt-3 max-w-xl text-2xl font-semibold tracking-tight sm:text-3xl">{video.title}</h1><button type="button" onClick={() => setIsPlaying((current) => !current)} className="mt-7 flex h-12 w-12 items-center justify-center rounded-full bg-white text-slate-950 transition hover:scale-105">{isPlaying ? <Pause size={18} fill="currentColor" /> : <Play size={18} fill="currentColor" className="ml-0.5" />}</button></div><div className="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-slate-950/95 to-transparent px-5 pb-4 pt-12"><div className="flex items-center gap-3"><button type="button" onClick={() => setIsPlaying((current) => !current)} className="text-white" aria-label={isPlaying ? 'Tạm dừng video' : 'Phát video'}>{isPlaying ? <Pause size={16} fill="currentColor" /> : <Play size={16} fill="currentColor" />}</button><input type="range" min="0" max="100" value={progress} onChange={(event) => setProgress(Number(event.target.value))} className="h-1.5 min-w-0 flex-1 cursor-pointer accent-teal-300" aria-label="Tiến độ video" /><span className="text-xs tabular-nums text-slate-300">{watchedLabel} / {video.duration}</span><button type="button" className="hidden text-slate-300 transition hover:text-white sm:block" aria-label="Phát lại từ đầu" onClick={() => setProgress(0)}><RotateCcw size={15} /></button></div></div></div>
        <div className="mt-6 flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-teal-700 dark:text-teal-300">{video.module} · {video.duration}</p><h2 className="mt-2 text-2xl font-semibold tracking-tight text-slate-900 dark:text-white">{video.title}</h2><p className="mt-3 max-w-2xl text-sm leading-7 text-slate-500 dark:text-slate-400">{video.description}</p></div><div className="flex shrink-0 flex-col gap-2 sm:items-end"><button type="button" onClick={markComplete} className={`inline-flex items-center justify-center gap-2 rounded-xl px-4 py-2.5 text-sm font-semibold transition ${isCompleted ? 'bg-teal-50 text-teal-700 dark:bg-teal-950/60 dark:text-teal-300' : 'bg-slate-900 text-white hover:bg-teal-700 dark:bg-teal-300 dark:text-slate-950 dark:hover:bg-teal-200'}`}>{isCompleted ? <CheckCircle2 size={16} /> : <Check size={16} />}{isCompleted ? 'Đã hoàn thành' : 'Đánh dấu hoàn thành'}</button><button type="button" onClick={handleNext} className="inline-flex items-center justify-center gap-2 rounded-xl border border-slate-200 px-4 py-2.5 text-sm font-semibold text-slate-600 transition hover:border-teal-300 hover:text-teal-700 dark:border-slate-800 dark:text-slate-300 dark:hover:border-teal-900 dark:hover:text-teal-300">{nextVideo ? 'Chuyển video tiếp theo' : 'Về Dashboard'} <ArrowRight size={15} /></button></div></div>
        <div className="mt-8 overflow-hidden rounded-3xl border border-slate-200 bg-white dark:border-slate-800 dark:bg-slate-900"><div className="flex overflow-x-auto border-b border-slate-200 dark:border-slate-800">{([['notes', 'Ghi chú', StickyNote], ['attachments', 'Tài liệu đính kèm', FileText], ['assistant', 'AI Assistant', Sparkles] ] as const).map(([tab, label, TabIcon]) => <button key={tab} type="button" onClick={() => setActiveTab(tab)} className={`inline-flex shrink-0 items-center gap-2 border-b-2 px-4 py-4 text-sm font-semibold transition sm:px-5 ${activeTab === tab ? 'border-teal-600 text-teal-700 dark:border-teal-300 dark:text-teal-300' : 'border-transparent text-slate-400 hover:text-slate-700 dark:hover:text-slate-200'}`}><TabIcon size={16} />{label}</button>)}</div>{activeTab === 'notes' && <div className="p-5 sm:p-6"><div className="flex items-start gap-3"><span className="flex h-9 w-9 shrink-0 items-center justify-center rounded-xl bg-amber-50 text-amber-600 dark:bg-amber-950/50 dark:text-amber-300"><StickyNote size={17} /></span><div><h3 className="text-sm font-semibold text-slate-900 dark:text-white">Ghi chú cá nhân</h3><p className="mt-1 text-xs text-slate-400">Lưu lại insight hoặc câu hỏi bạn muốn xem lại.</p></div></div><textarea value={notes} onChange={(event) => setNotes(event.target.value)} placeholder="Ví dụ: Kiểm tra lại nguồn dữ liệu trước khi tin vào cảnh báo của AI..." className="mt-5 min-h-32 w-full resize-y rounded-2xl border border-slate-200 bg-slate-50 p-4 text-sm leading-6 text-slate-700 outline-none transition placeholder:text-slate-400 focus:border-teal-400 focus:ring-2 focus:ring-teal-100 dark:border-slate-800 dark:bg-slate-950/50 dark:text-slate-200 dark:focus:border-teal-700 dark:focus:ring-teal-950" /><div className="mt-3 flex justify-end"><span className="text-xs text-slate-400">{notes.length} ký tự · tự lưu trong MVP</span></div></div>}{activeTab === 'attachments' && <div className="space-y-3 p-5 sm:p-6"><div className="flex items-center gap-3 rounded-2xl border border-slate-200 p-4 dark:border-slate-800"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-blue-50 text-blue-700 dark:bg-blue-950/60 dark:text-blue-300"><FileText size={18} /></span><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold text-slate-900 dark:text-white">Checklist đối soát dữ liệu.pdf</p><p className="mt-1 text-xs text-slate-400">PDF · 1.2 MB · Tài liệu bài học</p></div><button type="button" className="rounded-xl p-2 text-slate-400 transition hover:bg-slate-100 hover:text-teal-700 dark:hover:bg-slate-800 dark:hover:text-teal-300" aria-label="Tải tài liệu"><Download size={16} /></button></div><div className="flex items-center gap-3 rounded-2xl border border-slate-200 p-4 dark:border-slate-800"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-teal-50 text-teal-700 dark:bg-teal-950/60 dark:text-teal-300"><BookOpen size={18} /></span><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold text-slate-900 dark:text-white">Bộ dữ liệu giả lập tháng 03.xlsx</p><p className="mt-1 text-xs text-slate-400">XLSX · 248 KB · Thực hành</p></div><button type="button" className="rounded-xl p-2 text-slate-400 transition hover:bg-slate-100 hover:text-teal-700 dark:hover:bg-slate-800 dark:hover:text-teal-300" aria-label="Tải bộ dữ liệu"><Download size={16} /></button></div></div>}{activeTab === 'assistant' && <div className="p-5 sm:p-6"><div className="rounded-2xl bg-teal-50 p-4 dark:bg-teal-950/40"><div className="flex items-center gap-2 text-sm font-semibold text-teal-800 dark:text-teal-200"><Sparkles size={16} /> Gợi ý từ AI Assistant</div><p className="mt-3 text-sm leading-6 text-teal-900/80 dark:text-teal-100/80">Trong bài này, hãy chú ý cách phân biệt “điểm lệch cần kiểm tra” với “kết luận sai phạm”. AI giúp ưu tiên tín hiệu; người có trách nhiệm vẫn xác minh và phê duyệt.</p></div><div className="mt-5 flex flex-wrap gap-2"><button type="button" className="rounded-full border border-slate-200 px-3 py-2 text-xs font-medium text-slate-500 dark:border-slate-800 dark:text-slate-300">Tóm tắt bài học</button><button type="button" className="rounded-full border border-slate-200 px-3 py-2 text-xs font-medium text-slate-500 dark:border-slate-800 dark:text-slate-300">Cho ví dụ đối soát</button><button type="button" className="rounded-full border border-slate-200 px-3 py-2 text-xs font-medium text-slate-500 dark:border-slate-800 dark:text-slate-300">Kiểm tra hiểu bài</button></div><p className="mt-5 flex items-center gap-2 text-xs text-slate-400"><MessageCircle size={14} /> Mở nút “Hỏi AI” ở góc phải để trò chuyện theo ngữ cảnh bài học.</p></div>}</div>
      </div>
      <aside className="overflow-hidden rounded-3xl border border-slate-200 bg-white dark:border-slate-800 dark:bg-slate-900"><div className="flex items-center justify-between border-b border-slate-200 px-5 py-4 dark:border-slate-800"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-teal-700 dark:text-teal-300">Course playlist</p><h2 className="mt-1 text-lg font-semibold text-slate-900 dark:text-white">AI for Accounting</h2></div><span className="text-sm font-semibold text-slate-500 dark:text-slate-400">20%</span></div><div className="px-5 py-4"><div className="h-1.5 overflow-hidden rounded-full bg-slate-100 dark:bg-slate-800"><div className="h-full w-[20%] rounded-full bg-teal-600 dark:bg-teal-300" /></div><p className="mt-2 text-xs text-slate-400">{accountingVideos.filter((item) => item.completed).length + (isCompleted && !video.completed ? 1 : 0)} / {accountingVideos.length} bài đã hoàn thành</p></div><div className="border-t border-slate-100 dark:border-slate-800">{accountingVideos.map((item, index) => { const isCurrent = item.id === video.id; const itemCompleted = item.completed || (isCurrent && isCompleted); return <Link key={item.id} to={`/dashboard/learn/${item.id}`} className={`flex gap-3 border-b border-slate-100 p-4 transition last:border-0 dark:border-slate-800 ${isCurrent ? 'bg-teal-50/70 dark:bg-teal-950/30' : 'hover:bg-slate-50 dark:hover:bg-slate-800/50'}`}><span className={`flex h-9 w-9 shrink-0 items-center justify-center rounded-xl text-xs font-bold ${itemCompleted ? 'bg-teal-100 text-teal-700 dark:bg-teal-900/70 dark:text-teal-200' : isCurrent ? 'bg-teal-700 text-white dark:bg-teal-300 dark:text-slate-950' : 'bg-slate-100 text-slate-400 dark:bg-slate-800'}`}>{itemCompleted ? <Check size={15} strokeWidth={3} /> : `0${index + 1}`}</span><span className="min-w-0 flex-1"><span className={`block text-sm font-semibold leading-5 ${isCurrent ? 'text-teal-900 dark:text-teal-100' : 'text-slate-700 dark:text-slate-200'}`}>{item.title}</span><span className="mt-1 block text-xs text-slate-400">{item.duration} · {item.module.split(' · ')[1]}</span></span></Link> })}</div><div className="border-t border-slate-100 p-4 dark:border-slate-800"><p className="flex items-center gap-2 text-xs leading-5 text-slate-400"><Sparkles size={14} className="shrink-0 text-teal-600" /> AI sẽ gợi ý bài tiếp theo dựa trên tiến độ và kỹ năng cần ưu tiên.</p></div></aside>
    </div></section><AIChatbot /></>
}
